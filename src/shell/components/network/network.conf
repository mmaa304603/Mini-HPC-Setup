# Network Configuration

# Head Node Configuration
HEAD_NODE_IP="10.0.0.1"
HEAD_NETWORK_INTERFACE="enp2s0"
# HEAD_NETWORK_INTERFACE="enp0s8"

NETWORK_MASK="255.255.252.0"
NETWORK="10.0.0.0"
DHCP_START="10.0.1.100"
DHCP_END="10.0.1.255"

# GPU Node Configuration
GPU_NODE_IP="10.0.2.4"
GPU_NETWORK_INTERFACE="enP8p1s0"  # Actual interface name on Jetson Orin Nano

# Firewall Configuration (Head Node Only)
FIREWALL_ENABLED=true
FIREWALL_SERVICES=("dhcp" "tftp" "nfs" "slurm")
FIREWALL_PORTS=("9873")  # Warewulf port 