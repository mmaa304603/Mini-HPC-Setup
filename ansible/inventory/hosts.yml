all:
  children:
    headnode:
      hosts:
        headnode:
          ansible_host: 10.0.0.1
          ansible_user: root
    computenodes:
      hosts:
        compute1:
          ansible_host: 10.0.1.1
          ansible_user: root
        compute2:
          ansible_host: 10.0.1.2
          ansible_user: root
        compute3:
          ansible_host: 10.0.1.3
          ansible_user: root
    hpc:
      children:
        headnode:
        computenodes: 