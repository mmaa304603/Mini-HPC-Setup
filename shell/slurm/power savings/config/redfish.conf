# Redfish Configuration
# Default credentials - override these with environment variables:
# REDFISH_USERNAME, REDFISH_PASSWORD, REDFISH_PORT
REDFISH_USERNAME="root"
REDFISH_PASSWORD="calvin"
REDFISH_PORT="443"

# Retry settings
MAX_RETRIES=3
RETRY_DELAY=5  # seconds

# Logging
LOG_FILE="/var/log/power_save.log" 